<% if @vote.valid? %>
  // Bug with this JS if the slugified name includes any non alpha numeric characters
  $("#post_<%= @post.slug %>_votes").html("<%= @post.total_votes %> votes");
<% else %>
  alert('You can only vote on a post once!');
<% end %>

